# StrokepredictionML
An end-to-end binary classification pipeline using H2O.ai (GBM) to predict stroke risk under extreme class imbalance (&lt;5% cases). Prioritizing AUC over deceptive baseline accuracy, it features 5-fold cross-validation, SHAP feature interpretability, and a custom decision threshold (0.148) to align probabilities with real-world clinical utility.
